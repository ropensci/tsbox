
#' Defunct
#' @param x ts-boxable time series, an object of class `ts`, `xts`, `data.frame`, `data.table`, or `tibble`.
#' @param from from date, character string, `Date` or `POSIXct`
#' @param to to date, character string, `Date` or `POSIXct`.
#' @param template ts-boxable time series, an object of class `ts`, `xts`, 
#' @export
ts_window <- function(x, from = NULL, to = NULL, template = NULL){
  .Defunct("ts_span")
}
